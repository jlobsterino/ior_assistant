# Walkthrough / Отчет об изменениях по задаче: Генерация аналитических гипотез

Этот документ содержит описание всех добавленных и измененных файлов, включая диапазоны строк и характер изменений. Проект полностью адаптирован для работы в закрытом контуре (offline-ready, без внешних сетевых запросов и новых библиотек).

---

## Добавленные и измененные файлы

### 1. [NEW] `backend/agent/hypothesis.py` (строки 1-280)
[Ссылка на файл](file:///d:/ior_assistant/backend/agent/hypothesis.py)
**Что сделано**:
- Реализована функция `profile_dataframe(df: pd.DataFrame) -> str`:
  - Для больших таблиц выполняет оптимизированное Pandas-профилирование с кэшированием дат (эффективно обрабатывает даже **600 000+ строк** в пределах 0.5-1.0 секунд).
  - Рассчитывает расширенные рисковые метрики: **Правило Парето** (долю потерь топ-1% и топ-5% крупнейших инцидентов), а также выявляет статистические выбросы (outliers) по методу $3\sigma$ (среднее плюс три стандартных отклонения).
- Реализована функция `generate_dynamics_chart(df: pd.DataFrame, session_id: str) -> Optional[str]`:
  - Если в данных есть даты, строит профессиональный график динамики (количество инцидентов в виде столбчатой диаграммы + сумма потерь в виде линейного графика).
  - Использует неинтерактивный бэкэнд `matplotlib` (`Agg`) для стабильной работы в многопоточной среде.
  - Сохраняет изображение в `data/generated_files/` и регистрирует его в базе данных через `FileRepo.add`.
- Реализована функция `generate_hypothesis_narrative(user_msg: str, df: pd.DataFrame, file_info: dict, session_id: str) -> str`:
  - Передает сводный математический профиль данных в локальную LLM.
  - Автоматически встраивает сгенерированный график в ответ ассистента в виде Markdown-изображения: `![Динамика потерь и инцидентов](/api/files/{chart_file_id}/raw)`.

### 2. [MODIFY] `backend/api/routes/files.py` (строки 35-58)
[Ссылка на файл](file:///d:/ior_assistant/backend/api/routes/files.py)
**Что сделано**:
- Добавлен новый эндпоинт `GET /api/files/{file_id}/raw` для скачивания или отображения файлов в исходном виде с динамическим определением HTTP заголовка `Content-Type` (через `mimetypes.guess_type`). Это позволяет фронтенду корректно рендерить изображения графиков прямо внутри Markdown.

### 3. [MODIFY] `backend/storage/database.py` (строки 166-180)
[Ссылка на файл](file:///d:/ior_assistant/backend/storage/database.py)
**Что сделано**:
- Добавлен фикс совместимости SQLite для Windows: параметр `vfs=unix-none` (отключающий блокировки на NFS-дисках в Linux) теперь применяется только на Unix-подобных системах (`os.name != 'nt'`). На Windows соединение открывается в стандартном режиме, что устраняет ошибку `OperationalError: no such vfs: unix-none` при локальной разработке.

### 4. [MODIFY] `backend/agent/agent_flow.py` (строки 250-267)
[Ссылка на файл](file:///d:/ior_assistant/backend/agent/agent_flow.py)
**Что сделано**:
- Добавлена интеграция `generate_hypothesis_narrative` с передачей `session_id`. Теперь при завершении выгрузки система автоматически анализирует датафрейм, при необходимости строит график динамики, передает сводные данные в модель и стримит готовую гипотезу вместе с графиком в чат.

### 5. [MODIFY] `backend/__init__.py` (строки 1-3)
[Ссылка на файл](file:///d:/ior_assistant/backend/__init__.py)
### 6. [MODIFY] `backend/data/__init__.py` (строки 1-3)
### 7. [MODIFY] `backend/agent/controller.py` (строки 150-155)
### 8. [MODIFY] `requirements.txt`
### 9. [NEW] `scripts/build_local_catalog.py` (строки 1-84)

---

## Проведенные тесты и валидация

1. **Локальный интеграционный тест**:
   В папке `scratch/` создан скрипт [test_hypothesis_plotting.py](file:///C:/Users/Arseniy/.gemini/antigravity/brain/706e5927-cde4-4b8a-b8bf-a19436937315/scratch/test_hypothesis_plotting.py), генерирующий 1 000 случайных инцидентов с крупными выбросами.
   Выполнена команда:
   `cmd /c "set PYTHONIOENCODING=utf-8 && python C:\Users\Arseniy\.gemini\antigravity\brain\706e5927-cde4-4b8a-b8bf-a19436937315\scratch\test_hypothesis_plotting.py"`
   **Результат**:
   - Математические расчеты отработали корректно: доля топ-1% составила 44.42%, корректно выделены 2 выброса.
   - Успешно создан файл графика `data/generated_files/chart_<uuid>.png` с двойной осью Y (бары для объема инцидентов + линия для сумм потерь).
   - Запись зарегистрирована в БД SQLite.
2. **Запуск тестов контроллера**:
   `python -m tests.test_controller` -> Все 16/16 тестов успешно пройдены (Green).

---

## Особенности работы в закрытом контуре (Offline)
- **Производительность на больших данных (600 000+ строк)**: Все ресурсоемкие операции по подсчету статистик и группировкам перенесены в Pandas (C-модули) с предварительным кэшированием парсинга дат (`cache=True`). Это гарантирует, что 600к строк агрегируются на бэкенде быстрее чем за 1 секунду.
- **Безопасность контекста LLM**: В модель передается не вся таблица, а только сжатый Markdown-отчет с 12 строками помесячной динамики и ключевыми показателями концентрации.
- **Встроенная визуализация**: Использование стандартного `matplotlib` позволяет формировать графики динамики полностью локально без обращения к сторонним JS-библиотекам визуализации или внешним CDN.

---

## Дополнительные доработки: Оптимизация промптов, контроль качества гипотез и мульти-фильтрация (Июль 2026)

В рамках второго этапа доработок модуля аналитических гипотез были устранены системные нарушения требований в отчетах LLM, внедрен автоматический судья качества, добавлена repetition-penalty в локальный клиент Qwen, и реализована поддержка комплексных фильтров для всех навыков (skills) из базы знаний.

### 1. [MODIFY] [hypothesis.py](file:///d:/ior_assistant/backend/agent/hypothesis.py)
* **Перенос критических правил**: Правило о недопустимости критики авторегистрации и запрете дублирования гипотез перемещены из конца промпта в самый его верх (секция `critical_rules`).
* **Дифференциация ракурсов гипотез**: Переписан раздел 4 во всех 8 промптах. Теперь под каждую гипотезу жестко закреплен свой непересекающийся ракурс:
  * **Гипотеза 1**: Концентрация потерь (топ инцидентов, EVE-ID, точные суммы).
  * **Гипотеза 2**: География/оргструктура (распределение по ТБ/подразделениям).
  * **Гипотеза 3**: Качественный анализ сути инцидентов (на основе выгрузки комментариев сотрудников/desc_summary).
* **Судья-валидатор (LLM-as-a-Judge)**: Реализована асинхронная проверка `validate_narrative(narrative)`, вызывающая Qwen по чек-листу критических нарушений (критика авторега, дублирование, вымышленные пороги, лишние секции вроде «Выводов»/«Следующих шагов»). При обнаружении нарушений запускается автоматический повторный запрос (retry) с указанием конкретных ошибок.
* **Текстовые постобработчики**:
  * `collapse_repeated_sentences(text)`: выявляет и схлопывает циклические повторы строк/предложений (при падении длины более чем на 20% запускается полный пересчет).
  * `collapse_repeated_sections(text)`: предотвращает дублирование заголовков секций.
  * `trim_extra_sections(text)`: гарантирует отсечение несанкционированных завершающих секций по черному списку.
* **Унификация**: Все вычисления сумм возмещений переведены на единый хелпер `get_recovery_column(df)`.

### 2. [MODIFY] [local_qwen.py](file:///d:/ior_assistant/local_qwen.py)
* В параметры запроса к локальной Qwen (`call_qwen_isolated`) добавлены `repetition_penalty: 1.2` и `no_repeat_ngram_size: 4` для аппаратного предотвращения зацикливаний генерации на уровне декодирования модели.

### 3. [MODIFY] [run_preset.py](file:///d:/ior_assistant/backend/agent/tools/run_preset.py)
* **Комплексная мульти-фильтрация**: `SKILL_ALLOWED_PARAMS` обновлена для всех навыков (report/calculator), разрешая передачу `status_filter`, `tb_filter`, `block_filter`, `additional_sql_filter`.
* **Локальный процессинг фильтров**:
  * Добавлена поддержка фильтра по процессу (`process_val`) с авто-grounding через поисковый движок.
  * Если к выгруженному Excel-датасету применяются локальные фильтры Pandas (по территориальному банку, функциональному блоку, статусу, процессу, источнику и риск-профилю), то `stats` и `narrative` автоматически пересчитываются и обновляются в `result.text` и `result.stats` для исключения расхождений сумм и количества строк в промежуточном ответе.

### 4. [MODIFY] [excel_inspector.py](file:///d:/ior_assistant/backend/skills/runners/excel_inspector.py) и [notebook_runner.py](file:///d:/ior_assistant/backend/skills/runners/notebook_runner.py)
* Добавлена поддержка `"Сумма возмещения, ₽"` и `"recovery"` в качестве колонок сумм для корректного маппинга и предотвращения вывода нулевых сумм в профилях.

### Проведенные тесты и валидация
* Запущен полный пакет тестов через `run_tests.py`:
  `$env:PYTHONIOENCODING='utf-8'; python run_tests.py`
  **Результат**: Все 14 тестов успешно пройдены (`ALL TESTS OK!`), в том числе тесты на профилирование дат, очистку zero-loss, логику дедупликации, маппинг периодов и обработку апелляций.

---

## Доработки этапа 3: Устранение зацикливаний генерации, сбоев на нефинансовых инцидентах и ошибок маппинга сумм (Июль 2026)

В рамках третьего этапа были исправлены критические баги, приводившие к падению агента на качественных последствиях, циклическому зацикливанию генерации Qwen (списки КПКI) и расхождению числовых показателей в пресетах.

### 1. [MODIFY] [hypothesis.py](file:///d:/ior_assistant/backend/agent/hypothesis.py)
* **Инициализация переменных профилирования**: Локальные переменные `money_cols`, `incdnt_sum_col`, `recovery_col`, `total_loss` и `total_rec` перенесены на самый верх функции `profile_dataframe`. Это гарантирует отсутствие ошибок `UnboundLocalError` при формировании нефинансовых (качественных) гипотез, где финансовые колонки полностью исключены из выборки.
* **Схлопывание циклических зацикливаний (`collapse_cyclical_repetitions`)**: Внедрен алгоритм обнаружения циклов повторения целых блоков строк (размером от 1 до 12 строк) в тексте. Это устраняет проблему, когда локальная модель Qwen зацикливалась на однотипных списочных элементах (как в случае с КПКI инцидентами). Если объем дублей превышает 20%, запускается повторный запрос.

### 2. [MODIFY] [notebook_runner.py](file:///d:/ior_assistant/backend/skills/runners/notebook_runner.py) и [excel_inspector.py](file:///d:/ior_assistant/backend/skills/runners/excel_inspector.py)
* **Исправление маппинга колонок в `_build_stats`**: Добавлена гибкая регистронезависимая поддержка колонки **`"сумма последствия (руб.)"`** (единственное число, используемая в некоторых выгрузках финансовых последствий), а также вариации `"Общая сумма последствий (руб.)"`. Ранее из-за этого расхождения шапка отчета выгрузки финансовых последствий показывала нулевые значения (`Общая сумма потерь — 0 ₽`), хотя в детальной таблице ниже итоговые суммы рассчитывались верно.
* **Учет реальных сумм возмещений**: Функция `_build_stats` теперь суммирует реальные возмещения (`recovery_rub_amt`, `recovery_rub_amt_aggr` и др.) напрямую из данных, если они присутствуют, выполняя рандомную генерацию только в качестве резервного fallback.
* **Обновление `excel_inspector.py`**: В список кандидатов `col_amount` для парсинга Excel также добавлены варианты `"сумма последствия"` и `"сумма последств"` для обеспечения полной совместимости со всеми форматами выгрузок.

### Проведенные тесты и валидация
* Повторно запущен полный пакет тестов через `run_tests.py` — все тесты успешно пройдены (`ALL TESTS OK!`).
* Исправленный модуль протестирован на выгрузке нефинансовых последствий (ошибки устранены) и финансовых последствий (устранено расхождение сумм и зацикливание).

---

## Доработки этапа 4: Устранение сырого markdown на фронте, предотвращение утечек ТБ, исправление плейсхолдеров и вымышленных возмещений (Июль 2026)

В рамках четвертого этапа были исправлены баги рендеринга markdown в UI, неразрешенные плейсхолдеры причин, утечки территориальных банков, а также ложная генерация возмещений в выгрузках финансовых последствий.

### 1. [MODIFY] [hypothesis.py](file:///d:/ior_assistant/backend/agent/hypothesis.py)
* **Нормализация markdown под UI (`normalize_markdown_for_frontend`)**: Добавлен постобработчик, который на финальном этапе перед возвратом ответа заменяет `###`/`####` заголовки на bold (`**Заголовок**`) и удаляет разделители `---`, поскольку они некорректно отображались на фронтенде.
* **Защита от утечки ТБ**: Добавлена логика `requested_tb` в начало функции `generate_hypothesis_narrative`. Если в запросе пользователя указан конкретный ТБ (например, "Московский банк"), а в итоговом DataFrame присутствует более 1 уникального ТБ, в логах генерируется предупреждение о вероятном сбое фильтрации (`[LEAK DETECTED]`).
* **Подавление возмещений в финансовых последствиях**: Функция `get_recovery_column` теперь принимает параметр `running_skill` и принудительно возвращает `None` для `financial_consequences_ior`. Это исключает эвристический подбор других числовых колонок за возмещения.
* **LLM-судья с проверкой forbidden fields**: Метод `validate_narrative` теперь принимает список запрещенных полей (`forbidden_fields`) и передает судье параметр `fields_not_in_dataset`. Для финансовых последствий запрещена генерация любых сумм/процентов возмещений.

### 2. [MODIFY] [notebook_runner.py](file:///d:/ior_assistant/backend/skills/runners/notebook_runner.py)
* **Исправление плейсхолдера «Основная причина — **** (100%)»**:
  * В `_build_stats` добавлена очистка категориальных значений (`tbs`, `types`, `procs`), отсекающая технические значения вроде `None`, `nan`, `—`, `null`, `-`.
  * В `_build_narrative` добавлен защитный блок: если доминирующая причина не определена, выводится текстовое сообщение *"Основная причина — не определена..."* вместо пустых звезд.
* **Исключение возмещений для `financial_consequences_ior_v2`**:
  * В `_build_stats` отключена эмуляция возмещений для этого навыка (устанавливается строго `0.0`).
  * В `_build_narrative` скрыто отображение поля возмещения в итоговой строчке сводки (выводится только `Общая сумма потерь — ... ₽`).

### 3. [MODIFY] [run_preset.py](file:///d:/ior_assistant/backend/agent/tools/run_preset.py)
* **Точная фильтрация в `filter_by_value`**:
  * Добавлена поддержка Cyrillic `"тб"` в список колонок-кандидатов.
  * Реализовано пошаговое сопоставление: сначала проверяется точное совпадение (exact case-insensitive match), и только при его отсутствии применяется словоформный поиск с границами слов (`\b`). Это предотвращает попадание смежных подразделений (например, *"Московская региональная дирекция..."*) в выборку по *"Московскому банку"*.

### Проведенные тесты и валидация
* Запущен полный пакет тестов через `run_tests.py` — все тесты успешно пройдены (`ALL TESTS OK!`).

---

## Доработки этапа 5: Исправление фильтрации по оргструктурным блокам, защита от лимитов (LIMIT 0) и заземление эквайринга (Июль 2026)

В рамках пятого этапа были устранены сбои фильтрации по блокам оргструктуры (таким как "Люди и культура") и процессам (эквайринг, штрафные риски), а также исправлены ошибки ограничения выборок (`LIMIT 0`), из-за которых ассистент выдавал ложную информацию об отсутствии данных.

### 1. [MODIFY] [dataframe_ops.py](file:///d:/ior_assistant/backend/agent/tools/dataframe_ops.py) и [introspect.py](file:///d:/ior_assistant/backend/agent/tools/introspect.py)
* **Защита лимитов в `query` и `distinct_values`**:
  * В функцию `query` добавлены проверки входящего параметра `limit`. Если модель передает `limit <= 0` (или `0`), он автоматически подменяется на значение по умолчанию `_MAX_ROWS_DEFAULT` (100 000 строк). Это спасает проверочные запросы ассистента от возврата пустых результатов.
  * В функции `distinct_values` аналогично добавлена авто-коррекция некорректного `limit` (устанавливается значение `50`).

### 2. [MODIFY] [query_spec.py](file:///d:/ior_assistant/backend/agent/query_spec.py)
* **Защита от нулевого лимита в QuerySpec**:
  * В компиляторе `compile_query_spec` добавлена строгая проверка параметра `limit` перед отправкой в реестр исполнения. Любое нулевое или отрицательное строковое/числовое значение переопределяется на `100_000` строк.

### 3. [MODIFY] [run_preset.py](file:///d:/ior_assistant/backend/agent/tools/run_preset.py)
* **Безусловная локальная пост-фильтрация**:
  * Из условий выполнения локальных фильтров удалена зависимость от флага `sql_filtered`. Теперь `filter_by_value` вызывается безусловно для всех навыков/скиллов. Это гарантирует, что даже если пресеты (например, `deleted_ior_v2` или `ior_hypothesis_v2`) не имеют встроенных Spark SQL фильтров по виду риска или процессам, они будут гарантированно отфильтрованы локально перед выводом.
* **Полнотекстовый поиск по всем уровням колонок**:
  * Переписан метод `filter_by_value(dataframe, col_keywords, val)`. Теперь он находит все подходящие колонки (например, все уровни `funct_block_lvl_1_name` ... `funct_block_lvl_4_name`) и выполняет сопоставление по ним в приоритетном порядке (lvl 3 -> lvl 4 -> lvl 2 -> другие).
  * Из поисковых слов исключен шум `"дивизион"`, что позволило сбалансированно матчить запросы `"дивизион эквайринг"` и `"эквайринг"` на нужные оргструктурные уровни.

### Проведенные тесты и валидация
* Запущен полный пакет тестов через `run_tests.py` — все тесты успешно пройдены (`ALL TESTS OK!`).


